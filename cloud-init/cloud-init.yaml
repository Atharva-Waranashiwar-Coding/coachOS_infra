#cloud-config
package_update: true
packages: [ca-certificates, curl, git, jq]
runcmd:
  - install -d -m 0750 /opt/coachos /etc/coachos /var/backups/coachos
  - curl -fsSL https://get.docker.com | sh
  - systemctl enable --now docker
  - usermod -aG docker ubuntu
