# Canonical production application stack. The legacy root Compose file remains
# temporarily for backward compatibility while services are migrated here.
include:
  - ../docker-compose.prod.yml

volumes:
  certbot-webroot:
  letsencrypt:
