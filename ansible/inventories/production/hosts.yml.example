all:
  children:
    coachos:
      hosts:
        coachos-production:
          # Set this to: terraform output -raw public_ip
          ansible_host: REPLACE_WITH_TERRAFORM_OUTPUT
          ansible_user: deploy
          ansible_ssh_private_key_file: /Users/your-name/.ssh/coachos_deploy
